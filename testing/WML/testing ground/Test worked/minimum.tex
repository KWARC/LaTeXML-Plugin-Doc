\Para{Felder}{}
\Def{}{\BLAETTERDEF}
Sei $\widetilde M$ eine $n+1$-dimensionale Mannigfaltigkeit und 
$S\subset\RR$ eine Teilmenge.
$\{f_s\colon s\in S\}$ erfÅlle die folgenden Bedingungen:
\parindent = 1 true cm
\item{1.}FÅr alle $s\in S$ sind die $f_s\colon M\longrightarrow \widetilde M$
 $n$-dimensionale immersierte HyperflÑchen in $\widetilde M$.
\item{2.}$\widetilde M=\bigcup _{s\in S}{f_s(M)} $.
\item{3.}FÅr alle $s\ne t$ ist $f_s(M) \cap f_t(M) = \emptyset$. 
\item{4.}FÅr alle $p\in \widetilde M$ gibt es eine offene Umgebung 
$V\subset \widetilde M$ von $p$,
eine offene Menge $U\times (a,b)\subset \RR^n\times S$ und einen 
$\C^k$-Diffeomorphismus $f\colon U\times (a,b)\longrightarrow V$ so, 
daû $f(U\times\{s\})=V\cap M_s$ fÅr $s\in (a,b)$.
\parindent= 0 true cm
\par $\{f_s\colon s\in S\subset \RR\}$ heiût dann {\bf $\C^k$-BlÑtterung von
 $\widetilde M$ in $n$-dimensionale HyperflÑchen}.
\Def{}{\FELD}
Sei $\widetilde M$ eine $n+1$-dimensionale Mannigfaltigkeit, 
$f\colon M\longrightarrow \widetilde M$ eine  $n$-dimensionale kompakte 
orientierte immersierte Extremale zu einem 
Funktional $\F$ in $\widetilde M$  und
$\Phi=\{f_s\colon s\in S\}$ eine $\C^k$-BlÑtterung von $\widetilde M$ in $n$-dimensionale 
orientierbare Extremale von $\F$. 
\par $\Phi$ heiût {\bf $\C^k$-Feld zu $f$ in $\widetilde M$}, falls es eine
offene Untermannigfaltigkeit $N\subset M$ gibt, so daû
$f\bigl|_N=f_0\in \Phi$.
\par Man kann auf jedem $f_s(M)$ ein orientierendes Einheitsnormalenfeld 
$\nu_s$ so wÑhlen, daû $\nu $, definiert durch 
$\nu_p :=\nu_s(p)$ fÅr $p\in f_s(M)$,
 auf ganz $\widetilde M$ stetig ist und fÅr jede positiv orientierte
Orthonormalbasis $\{ x_1,\ldots,x_n\}$ von $\T _pf_s(M) $ die Orthonormalbasis
$\{\nu_p,x_1,\ldots,x_n\}$ von $\T _p\widetilde M$ positiv orientiert ist.
$\nu$ heiût dann das {\bf Einheitsnormalenfeld an $\Phi$}.

