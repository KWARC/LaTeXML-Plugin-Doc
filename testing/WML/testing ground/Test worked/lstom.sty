
\RequirePackage{listings}
\lstdefinelanguage{OpenMath}[]{XML}%
  {morekeywords={OMS,OMV,OMI,OMB,OMSTR,OMF,OMA,OMBIND,OMBVAR,OME,OMATTR,OMATP,OMOBJ},
   sensitive=true}
\lstdefinelanguage[om]{OpenMath}[]{XML}%
{morekeywords={om:OMS,om:OMV,om:OMI,om:OMB,om:OMSTR,om:OMF,om:OMA,om:OMBIND,om:OMBVAR,om:OME,om:OMATTR,om:OMATP,om:OMOBJ},
   sensitive=true}
\lstdefinelanguage{MathML2}[]{XML}%
{morekeywords={math,semantics,annotation-xml,annotation,
               mrow,mo,mi,mn,
               apply,bvar,ci,cn,sep,csymbol,
               condition,domainofapplication,lowlimit,uplimit,degree,
               interval,inverse,lambda,compose,ident,domain,codomain,image
               piecewise, piece, otherwise,
               quotient,factorial,divide,max,min,minus,plus,power,rem,times, root,gcd,lcm,
               and,or,xor,not,implies,forall,exists,
               abs,conjugate,arg,real,imaginary,floor,ceiling,
               sin,cos,tan,sec,csc,cot,sinh,cosh,tanh,sech,csch,coth,
               arcsin,arccos,arctan,arccosh,arccot,arccoth,arccsc,arccsch,arcsec,arcsech,arcsinh,arctanh,
               eq,neq,gt,lt,geq,leq,equivalent,approx,factorof,
               int,diff,partialdiff,divergence,grad,curl,laplacian,
               set,list,union,intersect,in,notin,subset,prsubset,notsubset,notprsubset,setdiff,card,cartesianproduct
               sum,product,limit,tendsto,exp,ln,log,mean,sdev,variance,median,mode,moment,momentabout
               vector,matrix,matrixrow,determinant,transpose,selector,vectorproduct,scalarproduct,outerproduct,
               integers,reals,rationals,naturalnumbers,complexes,primes,
               exponentiale,imaginaryi,notanumber,true,false,emptyset,pi,eulergamma,infinity,
              reln,fn,declare},
   sensitive=true}

\lstdefinelanguage{omCD}[]{OpenMath}%
  {morekeywords={CD,CDComment,CDName,CDBase,CDURL,CDDate,CDReviewDate,CDStatus,CDVersion,CDRevision,CDUses,
                 CDDefinition,Name,Description,CMP,FMP,Role,Example},
   sensitive=true}
\lstset{float=htb,columns=flexible,frame=lines,language=OpenMath,basicstyle=\scriptsize,
        numbers=left,stepnumber=5,numberstyle=\tiny,showstringspaces=false}
{}

\def\element#1{\texttt{#1}}
\def\symbol#1{\texttt{#1}}
\def\cdname#1{\texttt{#1}}
\def\psom#1{\mbox{\fbox{\ensuremath{#1}}}}

% LocalWords:  morekeywords OMV OMI OMSTR OMF OMA OMBIND OMBVAR OME OMATTR xml
% LocalWords:  OMATP OMOBJ mrow mn bvar ci cn csymbol domainofapplication eq lt
% LocalWords:  lowlimit uplimit forall partialdiff leq geq ne setdiff reln omCD
% LocalWords:  CDName CDURL CDReviewDate CDStatus CDVersion CDRevision CDUses
% LocalWords:  CDDefinition CMP FMP htb basicstyle stepnumber numberstyle sep
% LocalWords:  showstringspaces ident gcd lcm arg csc sinh tanh sech csch coth
% LocalWords:  arcsin arccos arctan arccosh arccot arccoth arccsc arccsch neq
% LocalWords:  arcsec arcsech arcsinh arctanh factorof laplacian notin prsubset
% LocalWords:  notsubset notprsubset cartesianproduct tendsto ln sdev matrixrow
% LocalWords:  momentabout vectorproduct scalarproduct outerproduct imaginaryi
% LocalWords:  naturalnumbers exponentiale notanumber emptyset eulergamma fn
